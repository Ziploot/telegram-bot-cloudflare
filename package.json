{
  "name": "telegram-bot-cloudflare",
  "version": "1.0.0",
  "description": "Serverless Telegram Bot deployed via Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
